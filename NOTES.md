# Turnstiles — Project Notes / Handoff

Theme-park visit tracker. Vanilla HTML/CSS/JS front-end + Supabase backend
(Postgres + Auth). Real accounts, persistent data. Static site, deployed on Vercel.

## Where things live
- Repo (source of truth): https://github.com/seancdaniel/turnstiles  (branch: main)
- **`redesign` branch — active front end redesign staging, started 2026-08-24.** Vercel auto-deploys every branch, so pushes to `redesign` get their own preview URL (check the Vercel dashboard's Deployments tab, filtered to the branch) without touching `goturnstiles.com`, which only tracks `main`. **While this branch exists and redesign work is in progress: commit and push to `redesign`, not `main`.** Only merge into `main` when explicitly asked to, since that's what actually goes live. If you're picking this up in a new session, check `git branch --show-current` before assuming which branch you're on. Background: two competing visual directions were mocked up first as standalone Artifacts (see the "Redesign concepts" entry further down) before any of this touched real code - the branch is for turning whichever direction gets picked into the actual site.
- Local folder: C:\Users\SeanDaniel\Desktop\Turnstiles
- Hosting: Vercel (auto-deploys from GitHub main)
- Backend: Supabase project https://guglgdsmqbtcvkmvxwrc.supabase.co
- **Custom domain is live**: `goturnstiles.com` (verified working — e.g. `img/` assets load fine from it). Email confirmation is ON, and Supabase Auth -> URL Configuration is fixed: Site URL is `https://goturnstiles.com`, Redirect URLs includes `https://goturnstiles.com/**`. **One thing not yet checked**: whether anyone signed up during the window when Site URL still pointed at the old Vercel URL and got a confirmation link to the wrong domain — worth a look at Supabase's Auth users list for anyone stuck unconfirmed from that period, if it matters.
- **Donations inbox**: `donations@goturnstiles.com` is live via Namecheap's free Email Forwarding (Domain List -> Manage -> Advanced DNS -> Mail Settings -> Email Forwarding -> add alias), forwarding to a dedicated Gmail account (not the personal Gmail). That Gmail account also has the address added under Settings -> Accounts and Import -> "Send mail as" (verified via the confirmation email, which arrives in the same forwarded inbox), so replies go out looking like they came from `donations@goturnstiles.com` with no separate SMTP server needed. Use that same Gmail account when signing up for a donations platform (see roadmap). `help@goturnstiles.com` is set up the same way, for technical support (linked from the About page's Need Help box and the footer Contact popup).
- **Monitoring**: Vercel Web Analytics is on (`<script defer src="/_vercel/insights/script.js">` in `index.html`'s `<head>` — the plain script-tag install, not the Next.js npm package the dashboard defaults to showing, since this isn't a Next.js project; also needs "Enable" clicked in the Vercel dashboard's Analytics tab, the script alone doesn't turn it on). Sentry error monitoring is also on (loader script right after the charset meta tag, error monitoring only — Logging/Session Replay/Tracing/Application Metrics were deliberately left off, see NOTES history/commit `2a9774a` for why). Supabase's free tier has **no automated backups** — worth remembering if something ever needs restoring.

## File architecture (SCRIPT LOAD ORDER MATTERS)
index.html loads scripts in this order, each with a `?v=N` cache-buster:
supabase-js CDN -> main.js -> restaurants-data.js -> supabase-auth.js -> supabase-data.js -> supabase-food.js -> supabase-festivals.js -> supabase-waittimes.js -> supabase-donors.js -> leaderboard.js

Later files intentionally OVERRIDE functions from earlier ones (plain function
declarations; the last definition wins). That is how the backend was layered on
without heavily rewriting main.js.

- **index.html** — markup: landing shell + app shell (views: **home** [the user's own profile: the `.cred` passholder-credential hero + the `.passport` ticket, see below], community, food, festivals, photos, waittimes, thanks, about) + modals. Bump `?v=` when editing a script/style.
- **styles.css** — all styles.
- **img/** — the project's first raster assets (everything before this was inline SVG/CSS). `margarita.png` (About page donate card background), `logo.png` (the real T/F monogram artwork, replaced the old hand-coded SVG compass mark in both header logo spots), `favicon.png` (the same logo composited onto the rounded navy-square card the old SVG favicon used). Source art lives outside the repo, usually at `C:\Users\SeanDaniel\Pictures\` or `...\Pictures\Turnstiles Images\`; there's no image editor in this environment, so processing (resize, background removal, compositing) is done with PowerShell's `System.Drawing`/`Bitmap` via the `LockBits` API. **Check for existing transparency before processing** (`Bitmap.GetPixel` on a corner, look at `.A`) — `margarita.png` arrived already background-removed and only needed a resize. When an image genuinely needs background removal, pick the technique by whether the art has legitimate white highlights to preserve: flood-fill from the image edges (leaves enclosed white regions like a disc inside a ring untouched, so only use it when that's what you want) vs. plain global near-white thresholding (simpler, but wrong for art with real white highlights like porcelain glare — used for `logo.png`, previously for the now-removed `coffee-cup.png`).
- **main.js** — demo/core: `STATE` (in-memory cache), toast/overlay/dropdown helpers, `showView` (accepts the legacy `'profile'` name and silently maps it to `'home'`, so old call sites still work), guest-browse, form helpers, renderers (`renderLeaderboard`, `renderCommunity`, `renderProfile`, `updatePassport`, `updateParkCounts`), `foodEmoji`, demo seed. **The Supabase client is created at the END of main.js**: `SUPABASE_URL`, `SUPABASE_KEY` (publishable), `const sb`. **Rank tiers** (`MONTHLY_TIERS`/`YEARLY_TIERS`, `getMonthlyTier`/`getYearlyTier`, `checkinsThisMonth`/`checkinsThisYear`, `tierEmblem`/`tierBadge`/`credPlateHtml`) also live here. **`avatarHtml(avatarUrl, emoji, extraClass)`** is the one place that decides emoji-vs-photo — every avatar-rendering call site across every file goes through it, see below.
- **supabase-auth.js** — real auth: `profileToUser`, `fetchProfile`, `enterApp`, `doSignIn`, `regSubmit`, `regNext`, `doSignOut`, `restoreSession` (stays logged in across refresh). **Login is by EMAIL**, not username.
- **supabase-data.js** — `loadData()` reads tables into STATE + rerenders; `rerenderActive`; `downscale`/`uploadPhoto` (Storage upload); overrides `enterApp`/`guestBrowse`; real `submitCheckin`/`submitFoodReview`/`submitPhoto`; `deleteCheckin`; `openUserProfile(userId)` (read-only view of another passholder — redirects to your own editable profile if `userId` is you). Clears the demo seed.
- **supabase-food.js** — overrides `loadData` (adds `spot`, `photo_url`, `food_favorites`, `wait_times`, `donors`, `festivals`/`festival_reviews`/`festival_favorites`); food search-and-pick; `updateRestaurantOptions`/`handleSpotSelect`/`setSpotValue`/`getSpotValue` (Park -> Restaurant cascading select for the "Where" field, backed by `RESTAURANTS_BY_PARK` in `restaurants-data.js` - see that file's own entry below); `submitFoodReview` (new + EDIT via `frEditId` + photo, auto-clears a matching "want to try" favorite on new submit); `getFoodAggregates`/`renderFood` grouped by name+park+spot (click a row -> `openFoodDetail` modal with per-review score/comment/photo + `openUserProfile` link); `renderMyFoodReviews` + `editFoodReview`/`deleteFoodReview` and `renderFoodFavorites` + `rateFavorite`/`removeFavorite`/`toggleFavoriteCurrent` ("Want to Try" list, both on profile); wraps `renderProfile`. **This is the file that owns `loadData()` now — if you add another table, extend the fetch here rather than overriding `loadData` yet again in a new file** (see `supabase-waittimes.js`/`supabase-festivals.js`, which deliberately do NOT re-override `loadData`).
- **supabase-festivals.js** — EPCOT Festivals, a separate space for festival-specific food reviews (Food & Wine, Flower & Garden, Festival of the Arts, Holidays, etc.) reachable via a hover/tap submenu off the "Food Scores" nav item (`.nav-has-sub`/`toggleNavSub`/`closeNavSub` in `main.js`+`styles.css` - CSS `:hover` on desktop, JS click-toggle fallback for touch, plus flat extra links in both mobile hamburger menus), so seasonal content never mixes into the year-round Food Scores page. **Reviews are about the food item, not the booth** — same shape as regular food reviews (`item_name` = what you're rating, `booth_name` = where you got it, optional) rather than the booth being the thing rated; got this backwards on the first pass (had it as "rate a booth", food review as an afterthought) and corrected it, including a schema migration since the tables were already live. "Current" festival is just `STATE.festivals[0]` (newest `created_at`, per the `order()` on that query in `supabase-food.js`'s `loadData`) - everything older is automatically the archive, no active/inactive flag to keep in sync. Form (`overlay-festival-review`): food item name (plain text with a `<datalist>` autocomplete off existing item names for the current festival - lighter than Food Scores' full search-and-pick UI, intentional given how much simpler this feature needs to be) + booth (optional, same idea, its own datalist), score, review, optional photo (dual-written to `photos` with `park:'EPCOT'`, same as food reviews, so festival shots also show up in Community Photos). `getFestivalAggregates(festivalId)`/`renderFestivalLists()` group by item name + booth (case-insensitive) and are reused for both the Current tab and whichever archived festival is being viewed; `renderFestivalView()` is the `showView('festivals')`/`rerenderActive()` entry point. Archive tab (`switchFestivalTab`) lists past festivals with a review count each; clicking one (`openFestivalArchive`/`closeFestivalArchiveDetail`) drills into that festival's own Top Rated/Most Reviewed lists read-only via the same renderer. `openFestivalDetail` (food detail modal, every individual review) hides the "Want to Try" button entirely when viewing an archived festival's item - favoriting only makes sense for the festival that's actually happening. "My Festival Reviews"/"Want to Try (This Festival)" (`renderMyFestivalReviews`/`renderFestivalFavorites`, current-festival-scoped only) live on the Festivals page itself, deliberately **not** folded into the merged home/profile page, per the same "don't clog it up" reasoning that keeps this whole feature off Food Scores. Starting a new festival is a one-line manual SQL insert (see `supabase/schema.sql`) - infrequent enough that a whole admin UI wasn't worth building, same call made for `donors`.
- **restaurants-data.js** — `RESTAURANTS_BY_PARK`: curated dining locations for all 31 park/resort options in the food review form's "Park or Resort" select (the 10 parks/water parks, **Disney Springs**, and the 20 Disney resort hotels - resort restaurants count too, people eat at 'Ohana and Boma). Same pattern as `RIDES_BY_PARK` in `supabase-waittimes.js`: a plain hardcoded list, no database table, loaded early (right after `main.js`) so `supabase-food.js` can read it. Each entry is `[name, type]` with `type` one of `'table'` / `'quick'` / `'snack'` (named walk-up spots only, deliberately excludes generic unnamed carts like "Popcorn Cart" - user's call when asked). Curated from general knowledge, not scraped from Disney/Universal's own live dining pages, so it **will** drift as restaurants open/close/rename - Epic Universe's section is the least certain since it's the newest park, and **Disney Springs is its own drift risk since that lineup rotates more than an actual theme park's does**. If a user reports something missing, closed, or misnamed, that's real: edit the array for that park and bump both `restaurants-data.js` and `supabase-food.js`'s `?v=` in `index.html`. Selecting "Other / not listed here..." reveals a plain text fallback (`#fr-spot-other`) rather than hard-blocking on gaps in the list - `getSpotValue()`/`setSpotValue()` in `supabase-food.js` handle reading/writing through that fallback transparently, including for older `food_reviews` rows whose `spot` text doesn't match any curated name. No schema change - `food_reviews.spot` still just stores plain text, same as before this existed.
- **supabase-waittimes.js** — `RIDES_BY_PARK` (curated per-park attraction rosters, park names must match the ones used elsewhere); `updateRideOptions` (Park -> Ride cascading select); `submitWaitTime`/`deleteWaitTime`; `renderWaitTimes` dispatches to `renderWaitTimesToday` (today's individual submissions, no averaging - live feed) and `renderWaitTimesYesterday` (per-ride average posted/actual wait, computed from yesterday's rows only); `switchWaitTimesTab`. "Today"/"yesterday" are just a filter on `created_at` against the viewer's local calendar day (`isSameLocalDay`) - nothing is ever archived or deleted, so a longer history is just a filter change away later.
- **supabase-donors.js** — Thank You page (`view-thanks`/`nav-thanks`, public, guest-browsable like Community/Food/Photos/Wait Times/About). `renderThanks()` builds the donor wall (`#donor-wall`) from `STATE.donors`; `donorChipHtml()` renders a linked chip (avatar + `@username`, click -> `openUserProfile`) for an entry with a live `user_id`, or a plain-text chip for one without. Does NOT re-override `loadData` — `STATE.donors` is populated in `supabase-food.js`'s fetch per the convention above. Admin-only "Add a Donor" panel (`#donor-admin-panel`, shown only when `STATE.currentUser.isAdmin`) toggles between picking a registered account (`switchDonorMode`/`donorUserSearch`/`donorPickEl`, same search-and-pick pattern as food items) and typing a plain display name for a non-account donor; `submitDonor()`/`deleteDonor()` insert/delete via RLS that only allows rows where `profiles.is_admin` is true for `auth.uid()` — enforced server-side, the client-side admin check is just UI convenience.
- **leaderboard.js** — `buildLeaderboard` override + `LB_GROUPS` (Disney/Universal); `selectResort`/`selectLbPark`; two-level filter (resort -> park). Water parks folded in: Blizzard Beach + Typhoon Lagoon -> Disney; Volcano Bay -> Universal.
- **api/notify-signup.js** — **live and confirmed working end-to-end** (tested with a real throwaway signup, email landed in `help@goturnstiles.com`). The site's first (and so far only) serverless function, not a static file - Vercel auto-detects anything under `api/` as a Node function, no build config needed. Sends Sean an email through Resend every time someone signs up. Not called by the front end at all - it's called by a **Supabase Database Webhook** (found under **Database -> Integrations** in this project's dashboard, not directly under "Database" - the feature's internal name is "Hooks", URL path `/database/hooks`, labeled differently in different places) on `profiles` INSERT, since `handle_new_user()` inserts exactly one profile row per real new signup. Protected by a shared-secret header (`x-webhook-secret`) since the endpoint URL itself is public like any Vercel route. See its own top-of-file comment for the three required Vercel environment variables (`RESEND_API_KEY`, `NOTIFY_EMAIL` - set to `help@goturnstiles.com`, `SIGNUP_WEBHOOK_SECRET`) and exactly what to configure in each dashboard - none of that setup can be done from this repo, it's dashboard-only on both the Vercel and Supabase side. **Two real gotchas hit while setting this up, worth knowing if this ever needs touching again**: (1) this Supabase project didn't have the `pg_net` extension enabled, which threw `ERROR: 3F000: schema "supabase_functions" does not exist` when first creating the webhook - enabling `pg_net` under Database -> Extensions fixed it, this is what provisions the internal trigger machinery Database Webhooks depend on; (2) Vercel does **not** retroactively apply newly-added/changed environment variables to an already-live deployment - adding the three env vars above only takes effect after a fresh Redeploy (Deployments -> the deployment -> ⋯ -> Redeploy), which is why the very first real test came back `401` even though everything was configured correctly by that point.

## Supabase
- Tables: `profiles` (has `avatar_url`, `disney_pass`/`universal_pass`, `is_admin`), `checkins`, `food_reviews` (has `spot`, `photo_url`), `photos`, `food_favorites`, `wait_times`, `donors`, `festivals`, `festival_reviews`, `festival_favorites`. See `supabase/schema.sql`.
- Trigger `on_auth_user_created` auto-creates a profile from signup metadata.
- RLS: public read on everything **except `food_favorites`/`festival_favorites`, which are private (own rows only)** — they're personal reminder lists, not social content; write/update/delete only your own rows everywhere. **Exceptions**: `donors` is public-read but admin-only write — insert/delete require `profiles.is_admin = true` for the calling user, checked in the policy itself (not just client-side); `is_admin` is a manually-set flag, not a real role system — see the Thank You entry below for how to make an account admin. `festivals` is public-read with **no write policy at all** — new festivals are only ever added by hand via a direct SQL insert (see the EPCOT Festivals entry below), there's no in-app way to create one.
- The publishable key in main.js is SAFE (public by design; RLS protects data). NEVER commit the service_role key or DB password.
- Email confirmation is ON. Site URL still needs fixing — see "Custom domain is live" above.
- **Custom SMTP is set up via Resend** (Authentication -> Settings -> SMTP Settings), replacing Supabase's rate-limited default mailer. Domain `goturnstiles.com` verified in Resend via DNS records at Namecheap (DKIM TXT on `resend._domainkey`, SPF TXT on `send` — the accompanying SES bounce-handling MX record was skipped since Namecheap won't allow adding MX records while Mail Settings is on "Email Forwarding," and it's not required for mail to send/authenticate correctly, just for bounce routing completeness). Sender is `noreply@goturnstiles.com` or similar (doesn't need to be a real inbox — it's send-only). **Confirm signup** and **Reset Password** templates are re-styled (Authentication -> Email Templates) to match the site's navy/parchment/coral look, using `<table>` + `bgcolor` attributes and a `color-scheme` meta tag rather than plain CSS `background-color` — Gmail's mobile app was auto-dark-mode-remapping the original colors (navy → olive/brown) without that.

## Conventions / gotchas
- After editing any .js/.css, bump its `?v=N` in index.html or the browser serves stale.
- Photos: new uploads are downscaled client-side then pushed to the `photos` Storage bucket via `uploadPhoto()`; `photos.image_url` stores the public Storage URL. Older rows created before this change still have base64 data URLs in `image_url` — both render identically via `<img src>`, no backfill needed.
- STATE is a client cache; call `loadData()` after any write to refresh + rerender.
- Multi-line edits to index.html/*.js were done via a small Python script (io.open read/replace/write) run through Bash — reliable for multi-line HTML.

## Roadmap (not done)
1. **Geolocation check-in verification**: `navigator.geolocation.getCurrentPosition` (one-time) -> geofence vs park coords -> set `checkins.verified = true`. Needs HTTPS (Vercel has it). The `verified` column already exists.
6. **"My Parks" ticket card — open design decision, don't just re-attempt blindly**: the current card (`.passport` in `index.html`/`styles.css`) is the original CSS-drawn vintage-ticket look (parchment gradient, engraved frame, dashed tear line, gold stamp, Admit One stub) — it lives in a **390px-wide sidebar** next to the home hero text (collapses to full-width, fluid, below 900px viewport), auto-height based on content (measured **~295px** with 1 park logged, up to **~414px** at the 5-park display cap, empty state **~331px** — so design/scale for roughly **390x350**, nearly square).
   - Two other approaches were tried this session and both got reverted: (a) a from-scratch CSS recreation of a hand-drawn parchment-ticket mockup (commit `acb5411`, still live — this IS the current design), and (b) swapping in the actual mockup image as a background with live text overlaid via percentage-positioned patches (commits `955a01b`/reverted by `bec5e1d`) — this needed the card to move out of the narrow sidebar into a full-width section since the mockup was a 3:2 landscape "main ticket + tear-off stub" shape that didn't fit 390px without distortion, and the result looked bad enough to revert on sight.
   - Before trying again: decide up front whether to (1) keep the narrow sidebar and design/generate a square-ish ~390x350 image for it (no side-by-side stub layout, would need a different way to show the "tear-off" motif — e.g. stub on the bottom instead of the side), or (2) commit to moving the card to a full-width section (like the About page's donate card) so a wide landscape ticket has room to read properly. Get that decision locked before generating new art or writing CSS.
7. **Color palette exploration — mockup built, tabled again, no decision made, nothing pushed.** User wants to see (not push) what the site would look like using the actual logo's colors — white, the gold from `img/logo.png`, and navy blue mixed into text — instead of the current parchment/coral/gold/navy scheme. Built and published a self-contained Artifact (not a repo change) — a toggle between the current scheme and the proposed one, applied to real copy/layout for nav, hero, a leaderboard card, and the "My Parks" ticket: parchment cream -> white, ticket-red -> navy (takes over the accent role currently held by coral - wordmark, ranks, tags, the hollow-outline "TURNSTILES" stroke), gold refined to the logo's sampled hex (`img/logo.png` averages ~`#D6A73C`; used `#C2962A` for text-weight contrast on white - turned out very close to the gold already in use). Structural navy (badges, banner strip) was left alone since it was already navy. User's reaction: "interesting" but tabled again for now, no direction chosen. If picking this back up: don't rebuild from scratch, ask whether they still have the Artifact link or want a fresh one, and pick up from the palette values above rather than re-deriving them.
8. **Front end redesign, in progress.** User wants the whole site to look "more modern, like a company paid real money for it," not just a color swap this time (bigger than item 7). Two full concepts were mocked up as a standalone Artifact (nav, sign-in ticket, home hero, leaderboard, all toggleable against the current site): **Concept A** ("Premium Ticket") keeps the vintage admission-ticket identity but disciplined - one gold accent instead of four competing colors, Fraunces added for polish. **Concept B** ("Modern SaaS") pivots to a clean warm off-white + amber + forest-green look with Bricolage Grotesque/Manrope, but keeps the real scalloped-edge admission ticket (same CSS mask technique the live site's guest landing page already uses) as the sign-in centerpiece, rebuilt in the new materials, after the user asked for it to stay and for the whole thing to feel bolder/less like a generic dashboard template. **No direction has been picked yet** - the `redesign` git branch (see "Where things live" above) exists so real implementation can happen with its own Vercel preview URL once one is chosen, but nothing has been built into actual site code yet as of this NOTES entry. Don't start implementing either concept into `index.html`/`styles.css` without the user explicitly saying which one (or what mix) to build.

### Done
3. **Launch hardening** — email confirmation is ON, and Supabase Auth's Site URL/Redirect URLs now correctly point at `https://goturnstiles.com` (was still the old Vercel URL with no Redirect URLs entries for a while, which is also why the password-reset redirect wasn't guaranteed to land right — see Password reset below). One unchecked loose end: whether anyone signed up during the stale-Site-URL window got a confirmation link to the wrong domain — worth a look at Supabase's Auth users list for anyone stuck unconfirmed from that period.
2. **Leaderboard tab styling** — resort/park filters now use the (previously unused) `.lb-tabs`/`.lb-tab` segmented-pill CSS instead of plain `btn-sm` buttons. See `leaderboard.js` `renderLbResortTabs`/`renderLbParkTabs`.
4. **Delete-a-review** — Delete buttons on food reviews (profile -> My Food Reviews) and check-ins (profile -> Visit History), own rows only via RLS. `deleteFoodReview()` in `supabase-food.js`, `deleteCheckin()` in `supabase-data.js`.
4. **Profile editing** — real modal (`overlay-edit-profile`) replaces the old `prompt()`-based edit: avatar, first/last name, username (uniqueness-checked), bio, location. `openEditProfile`/`submitEditProfile` in `supabase-auth.js`.
4. **Supabase Storage for photos** — new uploads (check-in photo, food-review photo, community photo share) now go to a `photos` Storage bucket instead of base64 data URLs in the DB; `uploadPhoto()` in `supabase-data.js`. Existing base64 rows still render fine (no backfill needed).
- **Mobile responsiveness** — the whole app (nav, hero, grids, modals, tables) now adapts down to ~375px: hamburger menu for the logged-in nav, stacking/wrapping layouts, horizontal-scroll tables. Fixed a latent `.modal { min-width: 0 }` flexbox bug along the way (a flex item's default `min-width: auto` let wide descendants force any modal wider than the viewport).
- **Click a food item -> see its reviews** — detail modal per item (score, comment, photo per reviewer). Required moving food-review photos onto the row itself (`food_reviews.photo_url`) instead of only the loosely-linked `photos` table.
- **View another passholder's profile** — read-only modal (bio, tier/badges, stats, recent visits, food reviews), reachable from the leaderboard, activity feed, food-review authors, and photo credits. Clicking your own name/avatar goes to your real editable profile instead.
- **"Want to Try" food favorites** — star an item from its detail modal to save it to a private per-user list shown on your profile; from there, "Rate It" opens the review form pre-filled, and submitting a new review for that item automatically clears it off the list (or remove it manually anytime). New `food_favorites` table, private via RLS.
- **Merged "My Parks" and "Profile" into one page** — the user considers them the same thing, so `view-profile` no longer exists as a separate screen. Its content (profile header card, stat cards, My Food Reviews, Want to Try, Visit History) now lives inside `view-home`, replacing the old "Recent Activity" feed section (which is gone — `renderRecentActivity` was deleted along with `#recent-activity-list`). The "My history" link and the dropdown's "My Profile" item were removed since they're redundant now. `showView('profile')` still works everywhere it's called (auth flows, food/favorite actions, `openUserProfile`) because `showView()` maps the name to `'home'` internally — don't reintroduce a `view-profile` element without also reverting that mapping.
- **Wait Times** — new top-level nav section. Log a park + curated ride + posted wait + actual wait (minutes); requires an account, same as check-ins/food reviews. "Today" tab is a live, ungrouped feed (own entries deletable) with no averaging by design — a same-day average would be a moving target. "Yesterday's Averages" tab computes per-ride average posted/actual wait from the previous calendar day's rows only. Day boundaries use the viewer's local clock, not park time. New `wait_times` table, public read, own-row insert/delete.
- **Two-track rank tiers, replacing the old single 4-tier Bronze/Silver/Gold/Platinum system** — no new table, purely computed client-side from existing `checkins.date`:
  - **Monthly** (resets naturally on the 1st, since it's just a filter on the current calendar month): First Year(1) -> Padawan(3) -> Quartermaster(5) -> Skipper(7) -> Prefect(9) -> Agent J(11) -> Captain(13) check-ins. Renamed from the original generic Bronze..Grandmaster set to theme-park/franchise references, matching the yearly ladder in spirit; icons were swapped to suit (the medal emoji stopped making sense) but **thresholds and colours are unchanged**, the colours being a legible progression already contrast-checked against the gold profile card.
  - **Yearly** (resets naturally on Jan 1st): Dopey(1) -> Park Hopper(5) -> Adventurer(12) -> Galaxy Defender(25) -> Galactic Hero(45) -> Tri-Wizard Cup(75) -> Club 33(120) check-ins. Names are real theme-park references on purpose (Buzz Lightyear's and Men in Black's max-score ride titles, the Wizarding World's Triwizard Cup, the actual real-life Club 33).
  - Both tiers show as a small colored `tierEmblem()` icon next to usernames (leaderboard rows, food-review authors) and as a full labeled `tierBadge()` pill on profiles (your own merged home/profile page and the read-only `openUserProfile` modal). `getMonthlyTier`/`getYearlyTier` return `null` below the first threshold - both helpers render an "Unranked" fallback rather than crashing.
  - **Yearly "Skipper" was renamed to "Adventurer"** when the monthly ladder took the name Skipper (Jungle Cruise) - two ranks with one name would have shown as identical plates side by side on the profile card. Check both ladders for collisions before renaming either. **Separate, still-live heads up**: the yearly tier name "Park Hopper" collides with the *existing, unrelated* achievement badge also called "Park Hopper" (visited 4+ distinct parks, in `renderProfile`/`openUserProfile`). Both can show on the same profile with identical text for different reasons - not fixed, just flagging it in case it reads as a bug later.
- **Uploaded profile photo, alongside the emoji avatars** — registration step 2 and Edit Profile both got an "Or Upload a Photo" zone next to the existing emoji grid. Whichever the user *last* interacted with wins (clicking any emoji clears a staged/saved photo - that's the removal mechanism, no separate "Remove" button); tracked via `regAvatarMode`/`epAvatarMode` ('emoji' | 'photo'). New `profiles.avatar_url` column, null unless a photo's been uploaded; `avatar` (emoji) is always still saved underneath as the fallback. Reuses the existing `photos` Storage bucket/policies (own-folder `<user_id>/...`), no new bucket. **Registration-time upload only works if email confirmation is off** (no session yet to authenticate the Storage write otherwise) — `regSubmit` checks `data.session` before attempting it and just silently skips to emoji-only if there isn't one. Now that confirmation is ON (see above), this is the live behavior for every new signup, not just an edge case: new users always start with the emoji avatar and have to add a photo afterward via Edit Profile once they've confirmed/signed in.
- **Beta notice** — small `.beta-badge` pill ("Beta - Actively Under Construction") on both the public landing hero and the logged-in home hero. Purely cosmetic, no logic.
- **Support/Donations section on the About page** — `.about-donate` card, sitting beside the hero text at the top of the About page (not buried at the bottom). Coral-to-gold gradient background with real artwork (`img/margarita.png`, previously a coffee cup) layered behind the text at 45% opacity plus a subtle text-shadow for legibility, replacing an earlier hand-drawn SVG cup doodle. Copy is intentionally low-pressure ("free for all users... never required"). The button ("Margarita Fund", previously "Buy Us a Coffee") links to the live Ko-fi page (`https://ko-fi.com/turnstiles`), set up as a PayPal-connected sole-proprietor Business account (Ko-fi doesn't hold funds itself — PayPal/Stripe underneath is required either way).
- **Community Photos organized by park, with a real lightbox** — same two-level resort -> park filter as the Leaderboard (`selectPhotoResort`/`selectPhotoPark` in `main.js`, reusing `leaderboard.js`'s `LB_GROUPS` directly rather than duplicating the park list). Clicking a thumbnail now actually does something (`openPhotoView` -> `overlay-photo-view`: full image, credited user linking to their profile, timestamp, caption) — previously it just sat there with a `cursor:pointer` that did nothing. **Deliberately no "food reviews" grouping**: food-review photos do land in the same `photos` table (dual-written from `submitFoodReview` alongside `food_reviews.photo_url`), but nothing distinguishes them from a standalone shared photo without a schema change — both are just `{park, caption}`, and caption text isn't a safe signal. If that's wanted later, it'd need an explicit source/kind column on `photos`.
- **Password reset** — "Forgot password?" link on Sign In (`openForgotPassword`/`submitForgotPassword` in `supabase-auth.js`, uses `sb.auth.resetPasswordForEmail`) and a "Change Password" button in Edit Profile, both funneling into one shared `overlay-change-password` modal reusing the registration flow's live strength checklist (`checkPasswordStrength()` is now parametrized by input/reqs element id, not hardcoded to the registration fields). Clicking the emailed reset link fires Supabase's `PASSWORD_RECOVERY` auth event, which force-opens that same modal with Close/Cancel hidden (no prior session to fall back to) until a new password is set, then signs them straight into the app the same way `doSignIn` does. **Heads up**: `resetPasswordForEmail`'s `redirectTo` uses `window.location.origin`, which has to be in Supabase Auth's allowed Redirect URLs list to actually work — ties into the Site-URL item in Launch Hardening below.
- **Monthly/Yearly leaderboards, for overall and each park** — a period selector (This Month / This Year / All-Time) on the Leaderboard page, orthogonal to the existing resort/park filter so any combination works. `buildLeaderboard(filter, period)` in `leaderboard.js` takes the new optional `period` arg; the one other caller (`updatePassport`'s rank stat) omits it and keeps all-time behavior.
- **Miles walked moved out of the check-in form** — logging a check-in no longer asks for miles (just park/date/review/photo); a hint in the modal points at Visit History. Any Visit History row without miles shows an "Add Miles" button that opens a small modal (`overlay-add-miles`) and does a plain `UPDATE checkins SET miles=...` (the "update own checkin" RLS policy already covered this, no schema change). Miles still defaults to 0 in the DB (`checkins.miles default 0`) if never added.
- **"Complete Check-In" prompt** — small card under the passport ticket (`.checkin-prompt`, in the new `.passport-col` wrapper alongside `.passport`) showing your most recent check-in, hidden entirely with zero check-ins. Auto-refreshes because `updatePassport` is wrapped in `supabase-data.js` to also call `renderCurrentCheckin()`, and `updatePassport` already runs after every checkin/miles/photo/food write via `rerenderActive()`. Its button (`openCurrentCheckin`) opens the same `overlay-add-miles` modal as Visit History's "Add Miles", now extended with a photo upload (saved to `photos`, same as the initial check-in) and a "Rate a Food Item" shortcut that hands off to the real food-review form pre-filled with that check-in's park (calls `resetFoodForm()` first so it can't reopen mid-edit).
- **Edit a check-in** — "Edit" button next to Delete on each Visit History row, reusing `overlay-checkin` (same `frEditId`-style pattern as food reviews: `ciEditId` tracks which row, `submitCheckin()` branches UPDATE vs INSERT). Covers park/date/review only — miles stays out of this form, edited separately via the Complete Check-In flow. Every "Log a Check-In" entry point (nav button, dropdown, hero button, `quickCheckin`) now routes through `resetCheckinForm()`/`newCheckin()` so a fresh check-in can never accidentally reopen mid-edit. Uploading a photo while editing still just adds a new `photos` row rather than replacing one - checkins don't store a photo reference to replace.
- **Security pass on user-controlled text** — main.js had several places (leaderboard, activity feed, photos grid/lightbox, `avatarHtml()`'s img src, a user's own visit history/passport bars) injecting username/caption/park text into `innerHTML` without escaping, unlike `supabase-food.js`/`supabase-data.js` which already used `escapeHtml()` consistently. All fixed. Also added `isValidUsername()` (letters/numbers/underscore/period, 3-20 chars) at registration and Edit Profile as input-side defense in depth.
- **Footer links wired up** — About/Privacy/Terms/Contact in both footer instances (landing page and logged-in app shell) used to go nowhere (`href="#"`). About now navigates properly (`guestBrowse`/`showView` depending on shell); Privacy, Terms, and Contact open new read-only modals (`overlay-privacy`, `overlay-terms`, `overlay-contact`) with real copy. Contact links to `help@goturnstiles.com`. The Privacy modal deliberately does not name Supabase by name (says "secure cloud storage" instead), at the user's request.
- **About page cleanup** — dropped the "★ The Official Passholder Tracker ★" eyebrow line, retitled the headline to "My Story" (was "Every visit counts."). Fixed the donate card looking stretched: `.about-top` had `align-items:stretch`, forcing `.about-donate` to match the height of the now much-taller two-paragraph intro text next to it; changed to `flex-start` so it sizes to its own content again. **Copy in `.about-lead` is the user's own words, inserted verbatim on request — do not "clean up" grammar, punctuation, or add em dashes to it without being asked.**
- **Rank Tiers legend on the Leaderboard** — card at the bottom of the Leaderboard tab listing all 7 Monthly and 7 Yearly tiers (icon, name, check-ins needed), built by `renderTierLegend()` in `main.js` straight from `MONTHLY_TIERS`/`YEARLY_TIERS` so it can't drift out of sync, wired into `renderCommunity()`.
- **Add to Home Screen box on the About page** — small card under the donate box (`.about-side-col` wraps both, same stacking technique as the passport ticket + check-in prompt) with a button opening `overlay-add-to-home`, plain iPhone (Safari Share -> Add to Home Screen) and Android (Chrome menu -> Add to Home screen/Install app) instructions. Styled with the site's gold (`var(--gold)`) background and bold dark text per user request, was plain white and hard to read at first.
- **Monitoring added** — Vercel Web Analytics and Sentry error monitoring (see "Where things live" above for details).
- **Fixed: bio/location never saving at signup** — `regSubmit()` (`supabase-auth.js`) used to write bio/location via a follow-up `sb.from('profiles').update(...)` call right after `signUp()`. With email confirmation ON there's no session yet at that point, so RLS silently dropped the update (0 rows matched, no error thrown) — bio/location the user typed on registration step 2 just vanished. Existing users who signed up during this window and never used Edit Profile afterward likely still have an empty bio/location in the DB; nothing to fix for them beyond telling them to re-enter it via Edit Profile (which *does* work, since it runs with a real session). Fix: bio/location now travel as `signUp()` metadata (like `username`/`first_name` always did) so the `handle_new_user()` Postgres trigger writes them straight into the new profile row, no session required. **Needs the updated `handle_new_user()` in `supabase/schema.sql` re-run against the live project** (see migration block at the bottom of the file) or new signups will still silently drop bio/location.
- **Disney/Universal Annual Pass fields** — two dropdowns (`disney_pass`/`universal_pass` columns) capturing which AP tier, if any, the user holds at each resort: set at registration (step 2, next to bio/location) and editable afterward in Edit Profile. Curated option lists (not free text) to match how the app already does curated lists (`RIDES_BY_PARK`, rank tiers): Disney = Pixie Dust / Pirate's / Sorcerer / Incredi-Pass / Other; Universal = Power / Preferred / Premier / Other. **These tier names do change occasionally — if a user says the list looks out of date, that's real, just update the `<option>`s in both the registration and Edit Profile selects in `index.html`.** Learned from the bio/location bug above, so these travel as `signUp()` metadata from the start rather than a risky follow-up `update()` call. Displayed as small badges (🏰 / 🌎) in the existing `.profile-badges` row on both your own profile (`renderProfile()` in `main.js`) and the read-only `openUserProfile()` modal (`supabase-data.js`) — omitted entirely if a user has no pass on file for that resort. Note: `location` still isn't displayed anywhere in the UI (pre-existing gap, out of scope here) — passes are, since they're more central to what this app is about.
- **Home hero, v2: the passholder credential** (supersedes the "My Profile header with tier medals" pass below, whose `.hero-profile-hd`/`.hero-medals`/`heroMedalHtml()` machinery is now deleted). The logged-in home hero was still wearing landing-page clothes on a page people open daily: a starburst "Official Passholder Tracker" eyebrow, a beta pill, a ~90px outlined `TURNSTILES` wordmark and an "EST. 2026" banner ate roughly 60% of the height, repeating a site name that is already in the nav 40px above - and the block was headed "My Profile" while containing **no** avatar, name or username. Rebuilt as `.cred`, a single card modelled on the annual pass card a passholder actually carries, in four bands: **issuing** (`img/logo.png` at 44px as the issuing mark + wordmark + Member Since), **identity** (avatar, name, @username, Disney/Universal AP pills), **standing** (the two rank plates), **figures** (the existing four stats). Ordered by what the user actually wants to know. Notes on the details, since they encode decisions rather than decoration:
  - **The four stat ids are unchanged** (`stat-visits`/`stat-miles`/`stat-foods`/`stat-rank`) - `updatePassport()` in `main.js` still writes them exactly as before, so that function needed no edit. Everything identity-shaped is written in `renderProfile()`.
  - `heroMedalHtml()` was replaced by **`credPlateHtml(tier, period, count)`**. Same tier names/icons/colors from `MONTHLY_TIERS`/`YEARLY_TIERS` - no data change - but each plate also shows the **check-in count that earned the tier** (`checkinsThisMonth()`/`checkinsThisYear()`, already computed to pick the tier). That was added because the plates otherwise had dead space; the count answers "why am I this rank?" for free.
  - **Rank is the only colored figure.** Visits/miles/foods are yours alone; rank is the only one that moves because of other people, so it alone carries the accent (`.cred-fig.is-rank`). The styling encodes that rather than decorating.
  - **The tier color lives on the icon disc only.** An accent rail and a tint wash were both tried and removed - three expressions of one fact.
  - **Deliberately crisp-edged**, against the scalloped vintage `.passport` ticket beside it. The contrast is doing work; don't "harmonise" them by making this one papery too.
  - **The card is a gold/brass pass card, and that is load-bearing, not decoration.** It was navy first, which blended into the navy page - the user flagged it. Their instinct was to go light; the reason it is *gold* and not parchment is that the `.passport` ticket sits directly beside it in parchment, and making this parchment too collapses the pair into one cream mass (mocked both up side by side to check - the parchment version is genuinely worse for that reason alone). Gold reads as a pass card next to a paper ticket. **Do not "harmonise" it back to cream.**
  - **Every colour on the card is a `--cd-*` token at the top of `.cred`**, so recolouring means editing one block rather than twenty rules. Added when the user said they wanted to tweak colours themselves. That includes `--cd-logo`: the logo art is baked gold and vanished against a gold card, so on this card only it is painted through the PNG as a CSS mask (`.cred-logo` is a `<span>`, not an `<img>`) and takes its colour from the token. The nav and landing logos are untouched `<img>` tags and stay gold on their dark grounds.
  - **Contrast on gold needed real care, and a naive checker lies about it twice.** (1) The small labels use a semi-transparent ink; a checker that reads `getComputedStyle().color` and ignores the alpha reports the solid value and passes them wrongly - composite against the ground first. (2) The card is a gradient, so "the background" is not one colour: the worst case is the stats band at the bottom, which is the gradient's dark end *plus* a dark overlay. `--cd-dim` had to go from `0.55` to `0.86` to clear 4.5:1 there. Everything on the card was re-checked against the ground at its own vertical position and passes (lowest is the rank figure at 3.17, which is large display text needing 3.0).
  - `.cred-beta` and the whole `.nextier` block sit **outside** the card on the dark page, so they stay page-coloured. Giving them a light fill to match the card just reads as muddy grey - tried it, it looks wrong.
  - `.cred*` CSS is written with **explicit colors rather than the light `:root` tokens**, because this block only ever renders inside the dark app shell. Empty state (new account: no visits, no tiers, no passes) is handled - plates fall back to a dashed "Unranked" disc, the AP pill row collapses to nothing, and the CTA reads "Log Your First Check-In".
  - **`.nextier` ("Next Tier") sits under the buttons**, added right after the redesign landed because the left column came out ~140px shorter than the passport ticket beside it, leaving a visible hole. It is not filler: the credential says where you stand, this says what is next, which is the obvious follow-on question. Two rows (monthly, yearly) built by **`nextTierRowHtml(ladder, count, period)`** straight from `MONTHLY_TIERS`/`YEARLY_TIERS`, so it cannot drift from the real thresholds. The bar measures progress **within the current rung** (from the tier you hold to the one above), not across the whole ladder, and takes the colour of the tier you are heading toward. Top of the ladder reads "Top tier reached" with a full bar; a brand-new account reads "1 to Bronze" / "1 to Dopey" at 0%. Balanced the columns to within ~12px.
  - Removed ~4.4KB of now-dead CSS: `.hero-title-block`, `.hero-starburst*`, `.hero-marquee-sign`, `.hero-name-outline`, `.hero-name-banner`, `.hero-banner-*`, `.hero-profile-hd`, `.hero-profile-lbl`, `.hero-medal*`, `.hero-stats`/`.hero-stat*` and their responsive overrides. **`.beta-badge` was deliberately kept** - the landing page still uses it (the app hero's copy became the small `.cred-beta` marker next to the buttons instead).
  - Verified against the landing page too, since CSS was deleted: it is untouched. **Separately spotted here, and fixed right after**: the landing ticket's "Sign In" button was near-invisible, because a second unscoped `.btn-hero-outline` rule (~line 341 of `styles.css`) sets cream text for the dark app hero and also lands on the ticket's ivory panel. A naive luminance checker gives a **false pass** on it, because the ticket panel is painted with a background-image gradient rather than a background-color, so walking the DOM for background-color finds the dark body instead - check that one visually. Fixed by scoping the cream override to `#app-shell` - the two buttons live in different shells, so that cleanly separates them.
- **Fixed: `.section-title` (the "MY FOOD REVIEWS" / "WANT TO TRY" / "VISIT HISTORY" / "SELECT A PARK" etc. headers) was a fixed 38px with no mobile scaling** — unlike `.hero-name-outline`/`.hero-tagline`, which already used `clamp()` for exactly this reason, `.section-title` had never gotten the same treatment and ran oversized on narrow phones. Changed to `font-size: clamp(24px, 7vw, 38px)`, same pattern as the others. `.subsection-title` (21px, Food Scores' "Top Rated"/"Most Reviewed") was already fine and wasn't touched.
- **New "Thank You" page — a public donor wall** — new top-level nav tab (`view-thanks`/`nav-thanks`, public/guest-browsable, added to both the logged-in nav and the separate landing-page guest nav) with a thank-you message up top and a wall of donor "chips" below. Each donor is either a real Turnstiles account (avatar + `@username`, clickable to their profile) or a plain display name for someone who donated without an account — **deliberately never a real name**, per request: for a linked account it's just their username (same as everywhere else in the app), and for an unlinked entry it's typed in exactly as it showed up on the donation. New `donors` table (`user_id` nullable, `display_name` always set as a snapshot so the wall survives a linked account later being deleted), public read via RLS. **Managed both ways, per request**: a tiny single-purpose admin system (`profiles.is_admin`, manually flipped true for one account - see `supabase/schema.sql`'s migration comment for the one-time SQL) drives an in-app "Add a Donor" panel on the page itself (`supabase-donors.js`: `switchDonorMode`/`donorUserSearch`/`submitDonor`/`deleteDonor`), so the site owner can add/remove donors without needing a code change - the admin check is enforced in the RLS policies themselves, not just hidden client-side. Asking Claude to add one still works too - it's just a normal insert into the `donors` table at that point, code-list editing wasn't needed once the table existed. Its copy was originally written with a couple of dashes; asked to remove them and never use dashes in copy going forward, saved as a standing memory.
- **One-time welcome modal on a new account's first entry into the app** — `overlay-welcome` (thanks for joining, site's still under construction, feedback wanted, `help@goturnstiles.com`, plus a "How To Add It" button opening the same `overlay-add-to-home` instructions the About page's "Keep Turnstiles a Tap Away" card uses - `openOverlay()` already closes whatever overlay is currently open before opening the next one, so no extra wiring was needed to swap from the welcome modal into the home-screen one) opens automatically the first time `enterApp()` runs for an account, driven by a new `profiles.welcomed` boolean that flips true right after (see `enterApp()` in `supabase-data.js`). Fires regardless of which path led into the app - immediate entry right after registration, or the first sign-in after confirming an email - since all of those funnel through the same `enterApp()`. **The migration's one-time backfill matters here**: existing accounts need `welcomed` force-set to `true` once (SQL in the migration's comment, not part of the safe-to-re-run block) or they'd all see a "welcome" message on their next login despite already being members - don't skip it, and don't ever fold that backfill into the reusable migration block since re-running it later would wipe the flag for anyone who'd signed up since but not yet logged in.
- **Structured Park -> Restaurant -> Item flow for food reviews**, replacing the old free-text "Where (stand/restaurant)" field. New `restaurants-data.js` (see its own entry above) backs a real cascading select grouped into Table Service / Quick Service / Snacks & Walk-Up, covering all 31 park+resort options the food form offers (30 originally, plus Disney Springs added right after), with an "Other / not listed here..." fallback so a gap in the curated list never blocks a submission. All the places that pre-fill the food form with a park+spot (search-and-pick, "Want to Try" -> Rate It, Edit, the Complete Check-In flow's "Rate a Food Item" shortcut) now correctly re-populate the restaurant dropdown for that park before setting the spot, including gracefully falling back to "Other" for older reviews whose spot text doesn't match a curated name. No schema change - still just plain text in `food_reviews.spot`.
- **Food Scores page rebuilt: parchment cards, search, and per-column pagination.** Restyled to match the profile credential's material language, then given the two things it was missing.
  - **Two columns, each paging on its own.** Top Rated and Most Reviewed sit side by side as separate lists, with independent page state (`foodPages = {score, count}`) and their own pager underneath. Paging one does not disturb the other. **Worth knowing the history so it does not get "fixed" back**: the first pass collapsed these into a single list with a sort toggle, on the reasoning that two paginators on one screen is confusing. The user asked for the two columns back, which is the call - and it reads fine in practice because each pager sits directly under its own labelled column rather than floating free.
  - **10 per page, prev/next arrows, ranks continuous across pages** (page 2 starts at 11). Pages clamp at both ends and buttons disable at the bounds.
  - **One search box filters the pool both columns draw from**, matching item name, park and restaurant - so "epcot" or "honeydukes" work, not just item names. Searching **resets both columns to page 1**; without that a smaller result set strands you past the end.
  - **Parchment (`.foodcard`), not the credential's gold.** Nothing on this page competes with it. Note this is exactly what was rejected for the *profile* page, and for a good reason that still holds there: on the profile the card sits beside the parchment `.passport` ticket and the pair collapses into one cream mass. There is no ticket here, so parchment is free. The class is named `.foodcard` rather than `.parchcard` so a future recolour does not make the name lie; its palette is the `--pc-*` block at the top of the rule.
  - `--pc-dim` is `0.78`, not the `0.70` it started at: at 0.70 the small labels landed on 4.57:1 against the darkest end of the card gradient, which passes but is too close to the line to survive someone tweaking the gradient later. Same alpha-compositing caveat as the credential applies - see that entry.
  - Rows are keyboard reachable (`tabindex`/`role="button"` + Enter/Space via `foodRowKey`), which the old `.food-item` rows were not.
  - **The Festivals list still uses the old white `.food-item` styling.** It shares that class, so restyling `.food-item` would have silently changed the Festivals page too. Left alone deliberately; matching it means giving it the same `.fl-*`/`.foodcard` treatment, not editing `.food-item`.
  - **Gotcha worth remembering**: `.fl-search` had `flex: 1 1 300px`, which is fine in a row but means a 300px *height* once the controls flip to `column` on mobile - it left a phone-screen-sized gap above the controls below it. DOM metrics reported "no overflow" and looked perfectly healthy; only a screenshot showed it.
- **New "EPCOT Festivals" section**, separate from the year-round Food Scores page. See the `supabase-festivals.js` entry above for the full mechanics (current-vs-archive, item+booth, the datalist autocomplete, Want to Try scoping). New `festivals`/`festival_reviews`/`festival_favorites` tables. **Requires running the migration in `supabase/schema.sql` against the live project before/immediately after this deploys** — the new fetches were added into the same `Promise.all()` as every other table in `loadData()` (`supabase-food.js`), so until those tables exist in the live database, the whole call rejects and the entire app's data silently fails to load (same deployment-ordering gotcha as every other migration this project has had - `donors`, `welcomed`, etc. - just worth restating since the blast radius here is "the whole site," not one feature). The migration seeds one starter festival ("EPCOT International Food & Wine Festival 2026") automatically. **Two follow-up fixes landed right after the initial version, before any real user data existed** — an invisible nav submenu link (CSS specificity collision with the unscoped `.nav-links a`/`.sh-nav a` dark-nav rules; fixed by bumping `.nav-submenu-inner a`'s selector) and a model correction (reviews were originally "rate a booth"; corrected to "rate the food, note the booth" with an accompanying `item_name`/nullable-`booth_name` schema migration — see below). **When a real new festival opens**, adding it is a one-line manual insert - no admin UI:
  ```sql
  insert into public.festivals (name) values ('EPCOT International Festival of the Arts 2027');
  ```
- **Fixed: check-ins, food reviews, and standalone photo shares could post twice** — a rapid double-click (or an impatient re-click on a slow connection) fired the whole async insert+photo-upload sequence a second time before the first call finished, since none of the submit handlers guarded against being invoked while already in flight. Fixed with a simple disable-the-button-during-the-request pattern across all four submit handlers: `submitCheckin`/`submitAddMiles`/`submitPhoto` (`supabase-data.js`) and `submitFoodReview` (`supabase-food.js`, the active one - there's also a dead, never-executed `submitFoodReview` earlier in `supabase-data.js` that supabase-food.js's definition overrides per the last-definition-wins load order, deliberately left alone). Each now takes the clicked button as a `btn` param (`onclick="submitCheckin(this)"` etc. in `index.html`), returns immediately if `btn.disabled`, disables it before the async work starts, and re-enables it in a `finally` block so it's never left stuck disabled on an error. New `.btn-sm:disabled` CSS in `styles.css` gives disabled buttons a dimmed, non-interactive look. Verified by scripting a real double-invoke (calling each handler twice back-to-back without awaiting the first, against a mocked Supabase client counting insert calls) and confirming exactly one row (and one photo, where applicable) landed per submission for all four handlers.

**Run the full migration block in `supabase/schema.sql` against the live project** — it's cumulative and safe to re-run; covers the delete-food policy, the Storage bucket, `food_reviews.photo_url`, the `food_favorites` table/policies, the `wait_times` table/policies, the `handle_new_user()` bio/location fix, the `disney_pass`/`universal_pass` columns, the `donors` table/`is_admin` column, the `welcomed` column, and the new `festivals`/`festival_reviews`/`festival_favorites` tables above. (The rank tier rework needed no schema change.) **Two one-time-only follow-ups after running it** (both called out in the migration comments - don't fold either into future full-file re-runs): set yourself as admin (`update public.profiles set is_admin = true where username = 'YOUR_USERNAME';`) or the Add Donor panel won't appear for you; and backfill everyone's `welcomed` flag (`update public.profiles set welcomed = true where welcomed = false;`) or every existing user will see the new-account welcome message on their next login. **The EPCOT Festivals tables are especially urgent to run promptly** — until they exist, `loadData()` fails entirely (see the EPCOT Festivals entry above), breaking the whole app, not just that one page.

## Resume in a new chat
Open a new Claude Code session in `C:\Users\SeanDaniel\Desktop\Turnstiles` and say "read NOTES.md and the code, then continue." The repo is the source of truth.
